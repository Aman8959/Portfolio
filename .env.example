# Add contact-provider credentials here in later phases. Never commit .env.local.
# CONTACT_API_KEY=
# Optional server-side endpoint for receiving contact submissions.
# CONTACT_WEBHOOK_URL=https://your-secure-form-handler.example/api/messages
