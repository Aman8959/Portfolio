export type Experience = {
  role: string;
  organization: string;
  period: string;
  summary: string;
  highlights: string[];
};

// Add only verified professional experience here.
export const experience: Experience[] = [];
